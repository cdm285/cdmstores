const translations = {
    pt: {
        "nav.home": "Início",
        "nav.products": "Produtos",
        "nav.support": "Atendimento",
        "nav.track": "Rastreio",
        "hero.title": "Eletrônicos Premium, Entrega Garantida.",
        "hero.subtitle": "Produtos de alta qualidade com entrega rápida, pagamento seguro e rastreio profissional.",
        "hero.cta": "Usar Cupom",
        "products.title": "Nossos Produtos",
        "products.p1.title": "Fone Bluetooth",
        "products.p1.desc": "Fone wireless de alta qualidade",
        "products.p2.title": "Carregador USB-C",
        "products.p2.desc": "Carregador rápido 65W",
        "products.p3.title": "Cabo Lightning",
        "products.p3.desc": "Cabo original certificado",
        "products.buy": "Adicionar ao Carrinho",
        "cart.my": "🛒 Meu Carrinho",
        "cart.empty": "Carrinho vazio",
        "cart.coupon": "Código do cupom",
        "cart.apply": "Aplicar",
        "cart.subtotal": "Subtotal",
        "cart.discount": "Desconto",
        "cart.shipping": "Frete",
        "cart.total": "Total",
        footer: "© 2026 CDM STORES · Todos os direitos reservados.",
        "checkout.title": "Finalizar Pagamento",
        "checkout.subtitle": "Aqui você poderá concluir seu pagamento com segurança.",
        "checkout.summaryLabel": "Resumo:",
        "checkout.summaryValue": "Cupom aplicado com sucesso.",
        "checkout.statusLabel": "Status:",
        "checkout.statusValue": "Selecione a forma de pagamento e conclua.",
        "checkout.selection": "Produto selecionado:",
        "checkout.payButton": "Pagar com Stripe",
        "checkout.back": "← Voltar para a loja",
        "tracking.title": "Rastrear Pedido",
        "tracking.subtitle": "Digite o código de rastreio enviado para você.",
        "tracking.placeholder": "Ex: CDM123456BR",
        "tracking.button": "Rastrear",
        "tracking.found": "Código localizado.",
        "tracking.status": "Status:",
        "tracking.eta": "Previsão:",
        "tracking.notFound": "Não encontramos esse código.",
        "tracking.help": "Revise o código ou fale com nosso atendimento.",
        "auth.login_title": "Entrar",
        "auth.login_button": "Entrar",
        "auth.register_title": "Criar Conta",
        "auth.register_button": "Criar Conta",
        "auth.email": "Email",
        "auth.password": "Senha",
        "auth.name": "Nome",
        "auth.phone": "Telefone",
        "auth.signup_link": "Não tem conta? Registre-se",
        "auth.login_link": "Já tem conta? Entrar",
        "auth.google": "Entrar com Google",
        "auth.facebook": "📘 Entrar com Facebook",
        "auth.user_login": "👤 Entrar",
        "chat.open": "Chat",
        "chat.placeholder": "Digite sua mensagem...",
        "chat.welcome": "Olá! Como posso ajudar?",
        "chat.fallback": "Anotei sua mensagem. Nossa equipe vai ajudar já já.",
        "chat.send": "Enviar"
    },
    en: {
        "nav.home": "Home",
        "nav.products": "Products",
        "nav.support": "Support",
        "nav.track": "Tracking",
        "hero.title": "Premium Electronics, Guaranteed Delivery.",
        "hero.subtitle": "High-quality products with fast shipping, secure payment and professional tracking.",
        "hero.cta": "Use Coupon",
        "products.title": "Our Products",
        "products.p1.title": "Bluetooth Headphones",
        "products.p1.desc": "High-quality wireless headphones",
        "products.p2.title": "USB-C Charger",
        "products.p2.desc": "Fast 65W Charger",
        "products.p3.title": "Lightning Cable",
        "products.p3.desc": "Original certified cable",
        "products.buy": "Add to Cart",
        "cart.my": "🛒 My Cart",
        "cart.empty": "Empty cart",
        "cart.coupon": "Coupon code",
        "cart.apply": "Apply",
        "cart.subtotal": "Subtotal",
        "cart.discount": "Discount",
        "cart.shipping": "Shipping",
        "cart.total": "Total",
        footer: "© 2026 CDM STORES · All rights reserved.",
        "checkout.title": "Checkout",
        "checkout.subtitle": "Finish your payment securely.",
        "checkout.summaryLabel": "Summary:",
        "checkout.summaryValue": "Coupon applied successfully.",
        "checkout.statusLabel": "Status:",
        "checkout.statusValue": "Choose Stripe to finish.",
        "checkout.selection": "Selected product:",
        "checkout.payButton": "Pay with Stripe",
        "checkout.back": "← Back to store",
        "tracking.title": "Track Order",
        "tracking.subtitle": "Enter the tracking code sent to you.",
        "tracking.placeholder": "Ex: CDM123456BR",
        "tracking.button": "Track",
        "tracking.found": "Code found.",
        "tracking.status": "Status:",
        "tracking.eta": "ETA:",
        "tracking.notFound": "We could not find this code.",
        "tracking.help": "Check the code or contact support.",
        "auth.login_title": "Login",
        "auth.login_button": "Login",
        "auth.register_title": "Create Account",
        "auth.register_button": "Create Account",
        "auth.email": "Email",
        "auth.password": "Password",
        "auth.name": "Name",
        "auth.phone": "Phone",
        "auth.signup_link": "Don't have an account? Sign up",
        "auth.login_link": "Already have an account? Login",
        "auth.google": "Login with Google",
        "auth.facebook": "📘 Login with Facebook",
        "auth.user_login": "👤 Login",
        "chat.open": "Chat",
        "chat.placeholder": "Type your message...",
        "chat.welcome": "Hi! How can I help?",
        "chat.fallback": "Got it. Our team will help soon.",
        "chat.send": "Send"
    },
    es: {
        "nav.home": "Inicio",
        "nav.products": "Productos",
        "nav.support": "Atención",
        "nav.track": "Seguimiento",
        "hero.title": "Electrónicos Premium, Entrega Garantizada.",
        "hero.subtitle": "Productos de alta calidad con envío rápido, pago seguro y rastreo profesional.",
        "hero.cta": "Usar cupón",
        "products.title": "Nuestros productos",
        "products.p1.title": "Auriculares Bluetooth",
        "products.p1.desc": "Auriculares inalámbricos de alta calidad",
        "products.p2.title": "Cargador USB-C",
        "products.p2.desc": "Cargador rápido 65W",
        "products.p3.title": "Cable Lightning",
        "products.p3.desc": "Cable original certificado",
        "products.buy": "Agregar al carrito",
        "cart.my": "🛒 Mi carrito",
        "cart.empty": "Carrito vacío",
        "cart.coupon": "Código de cupón",
        "cart.apply": "Aplicar",
        "cart.subtotal": "Subtotal",
        "cart.discount": "Descuento",
        "cart.shipping": "Envío",
        "cart.total": "Total",
        footer: "© 2026 CDM STORES · Todos los derechos reservados.",
        "checkout.title": "Pagar",
        "checkout.subtitle": "Finaliza tu pago con seguridad.",
        "checkout.summaryLabel": "Resumen:",
        "checkout.summaryValue": "Cupón aplicado con éxito.",
        "checkout.statusLabel": "Estado:",
        "checkout.statusValue": "Elige Stripe para finalizar.",
        "checkout.selection": "Producto seleccionado:",
        "checkout.payButton": "Pagar con Stripe",
        "checkout.back": "← Volver a la tienda",
        "tracking.title": "Rastrear pedido",
        "tracking.subtitle": "Ingresa el código de rastreo que recibiste.",
        "tracking.placeholder": "Ej: CDM123456BR",
        "tracking.button": "Rastrear",
        "tracking.found": "Código localizado.",
        "tracking.status": "Estado:",
        "tracking.eta": "Entrega:",
        "tracking.notFound": "No encontramos este código.",
        "tracking.help": "Revisa el código o habla con soporte.",
        "auth.login_title": "Ingresar",
        "auth.login_button": "Ingresar",
        "auth.register_title": "Crear Cuenta",
        "auth.register_button": "Crear Cuenta",
        "auth.email": "Email",
        "auth.password": "Contraseña",
        "auth.name": "Nombre",
        "auth.phone": "Teléfono",
        "auth.signup_link": "¿No tienes cuenta? Regístrate",
        "auth.login_link": "¿Ya tienes cuenta? Ingresar",
        "auth.google": "Ingresar con Google",
        "auth.facebook": "📘 Ingresar con Facebook",
        "auth.user_login": "👤 Ingresar",
        "chat.open": "Chat",
        "chat.placeholder": "Escribe tu mensaje...",
        "chat.welcome": "¡Hola! ¿Cómo puedo ayudar?",
        "chat.fallback": "Recibí tu mensaje. Te ayudaremos pronto.",
        "chat.send": "Enviar"
    }
};

const products = {
    p1: { price: 189.9, stripeLink: "https://buy.stripe.com/test_6oE5mTgBi1mP2owbII" },
    p2: { price: 219.9, stripeLink: "https://buy.stripe.com/test_cN28zJaBO9tz36E3cc" },
    p3: { price: 249.9, stripeLink: "https://buy.stripe.com/test_cN22a95amfUr1MAfZ0" }
};

const trackingDB = {
    CDM123456BR: {
        status: {
            pt: "Saiu para entrega",
            en: "Out for delivery",
            es: "En camino"
        },
        eta: "Hoje até 18h",
        lastUpdate: "2026-02-20 14:05"
    },
    CDM654321BR: {
        status: {
            pt: "Em separação",
            en: "Preparing order",
            es: "Preparando pedido"
        },
        eta: "Próximas 24h",
        lastUpdate: "2026-02-19 10:40"
    }
};

const state = {
    lang: localStorage.getItem("cdm_lang") || detectLang(),
    selectedProduct: null
};

function detectLang() {
    const nav = navigator.language || navigator.userLanguage;
    if (!nav) return "pt";
    if (nav.toLowerCase().startsWith("es")) return "es";
    if (nav.toLowerCase().startsWith("en")) return "en";
    return "pt";
}

function applyTranslations(lang) {
    state.lang = lang;
    localStorage.setItem("cdm_lang", lang);
    document.documentElement.lang = lang;

    // Traduzir elementos com data-i18n
    document.querySelectorAll("[data-i18n]").forEach((el) => {
        const key = el.dataset.i18n;
        const text = translations[lang]?.[key];
        if (text) el.textContent = text;
    });

    // Traduzir placeholders
    document.querySelectorAll("[data-i18n-placeholder]").forEach((el) => {
        const key = el.dataset.i18nPlaceholder;
        const text = translations[lang]?.[key];
        if (text) el.setAttribute("placeholder", text);
    });

    // Traduzir cart modal
    const cartHeader = document.querySelector(".cart-modal-header h2");
    if (cartHeader) cartHeader.textContent = translations[lang]?.["cart.my"] || "🛒 Meu Carrinho";
    
    const cartEmpty = document.querySelector(".cart-empty");
    if (cartEmpty) cartEmpty.textContent = translations[lang]?.["cart.empty"] || "Carrinho vazio";

    const couponInput = document.getElementById("coupon-input");
    if (couponInput) couponInput.placeholder = translations[lang]?.["cart.coupon"] || "Código do cupom";

    const couponBtn = document.querySelector(".btn-coupon");
    if (couponBtn) couponBtn.textContent = translations[lang]?.["cart.apply"] || "Aplicar";

    // Traduzir labels do carrinho
    const cartLabels = document.querySelectorAll(".cart-total p");
    if (cartLabels.length >= 3) {
        cartLabels[0].innerHTML = `${translations[lang]?.["cart.subtotal"] || "Subtotal"}: <strong>R$ <span id="cart-subtotal">0.00</span></strong>`;
        cartLabels[2].textContent = `${translations[lang]?.["cart.shipping"] || "Frete"}: R$ 15.00`;
    }
    const cartTotalH3 = document.querySelector(".cart-total h3");
    if (cartTotalH3) cartTotalH3.textContent = `${translations[lang]?.["cart.total"] || "Total"}: R$ ${document.getElementById("cart-total-amount")?.textContent || "15.00"}`;

    // Traduzir produtos (se existirem na página)
    document.querySelectorAll(".card h3").forEach((el, idx) => {
        const prodNum = idx + 1;
        const titleKey = `products.p${prodNum}.title`;
        const text = translations[lang]?.[titleKey];
        if (text) el.textContent = text;
    });

    document.querySelectorAll(".card p").forEach((el, idx) => {
        const prodNum = Math.floor(idx / 2) + 1; // 2 p tags per card
        const descKey = `products.p${prodNum}.desc`;
        const text = translations[lang]?.[descKey];
        if (text && el.textContent.includes("R$")) {
            const price = el.textContent.match(/R\$ [\d,]+/)?.[0] || "";
            el.innerHTML = `${price} - ${text}`;
        }
    });

    // Traduzir botões do carrinho
    document.querySelectorAll(".btn-card").forEach((btn) => {
        if (btn.textContent.includes("Adicionar") || btn.textContent.includes("Add") || btn.textContent.includes("Agregar")) {
            btn.textContent = translations[lang]?.["products.buy"] || "Adicionar ao Carrinho";
        }
    });

    // Traduzir auth modal se existir
    const loginForm = document.getElementById("login-form");
    const registerForm = document.getElementById("register-form");
    
    if (loginForm) {
        const loginTitle = loginForm.querySelector("h2");
        if (loginTitle) loginTitle.textContent = translations[lang]?.["auth.login_title"] || "Entrar";
        
        const loginBtn = loginForm.querySelector("button[type='submit']");
        if (loginBtn) loginBtn.textContent = translations[lang]?.["auth.login_button"] || "Entrar";
        
        const emailInput = loginForm.querySelector("input[type='email']");
        if (emailInput) emailInput.placeholder = translations[lang]?.["auth.email"] || "Email";
        
        const passwordInput = loginForm.querySelector("input[type='password']");
        if (passwordInput) passwordInput.placeholder = translations[lang]?.["auth.password"] || "Senha";
        
        const signupLink = loginForm.querySelector("button[type='button']");
        if (signupLink) signupLink.textContent = translations[lang]?.["auth.signup_link"] || "Não tem conta? Registre-se";
    }

    if (registerForm) {
        const registerTitle = registerForm.querySelector("h2");
        if (registerTitle) registerTitle.textContent = translations[lang]?.["auth.register_title"] || "Criar Conta";
        
        const registerBtn = registerForm.querySelector("button[type='submit']");
        if (registerBtn) registerBtn.textContent = translations[lang]?.["auth.register_button"] || "Criar Conta";
        
        const inputs = registerForm.querySelectorAll("input");
        inputs.forEach((input) => {
            if (input.type === "email") input.placeholder = translations[lang]?.["auth.email"] || "Email";
            if (input.type === "password") input.placeholder = translations[lang]?.["auth.password"] || "Senha";
            if (input.type === "text" && input.name === "name") input.placeholder = translations[lang]?.["auth.name"] || "Nome";
            if (input.type === "text" && input.name === "phone") input.placeholder = translations[lang]?.["auth.phone"] || "Telefone";
        });
        
        const loginLink = registerForm.querySelector("button[type='button']");
        if (loginLink) loginLink.textContent = translations[lang]?.["auth.login_link"] || "Já tem conta? Entrar";
    }

    // Traduzir botões OAuth
    const googleBtn = document.getElementById("google-login-btn");
    if (googleBtn) googleBtn.textContent = "🔷 " + (translations[lang]?.["auth.google"] || "Entrar com Google");
    
    const facebookBtn = document.getElementById("facebook-login-btn");
    if (facebookBtn) facebookBtn.textContent = translations[lang]?.["auth.facebook"] || "📘 Entrar com Facebook";

    // Traduzir FAB login (se existir)
    const fabLogin = document.getElementById("fab-login");
    if (fabLogin) fabLogin.title = translations[lang]?.["auth.user_login"] || "👤 Entrar";

    // Traduzir FAB chat (se existir)
    const fabChat = document.getElementById("fab-chat");
    if (fabChat) fabChat.title = translations[lang]?.["chat.open"] || "💬 Chat";

    // Traduzir chat
    const chatInput = document.getElementById("cdm-chat-input");
    if (chatInput) chatInput.setAttribute("placeholder", translations[lang]?.["chat.placeholder"] || "Digite sua mensagem...");
    
    const chatSend = document.getElementById("cdm-chat-send");
    if (chatSend) chatSend.textContent = translations[lang]?.["chat.send"] || "Enviar";
    
    const chatHeader = document.querySelector(".cdm-chat-header");
    if (chatHeader) chatHeader.textContent = "💬 CDM " + (translations[lang]?.["chat.open"] || "Chat");

    // Limpar e reinicializar mensagens do chat com novo idioma
    const chatBody = document.getElementById("cdm-chat-body");
    if (chatBody && chatBody.children.length > 0) {
        chatBody.innerHTML = '';
        const appendMsg = (text, cls = "") => {
            const p = document.createElement("div");
            p.className = `cdm-chat-msg ${cls}`;
            p.textContent = text;
            chatBody.appendChild(p);
            chatBody.scrollTop = chatBody.scrollHeight;
        };
        appendMsg(translations[lang]?.["chat.welcome"] || "Olá!", "bot");
    }

    updateCheckoutSelection();
}

function initLanguageSwitcher() {
    const selector = ".desktop-lang-btn[data-lang], .mobile-lang-btn[data-lang]";
    const buttons = document.querySelectorAll(selector);
    buttons.forEach((btn) => {
        btn.addEventListener("click", () => {
            applyTranslations(btn.dataset.lang);
            document.querySelectorAll(selector).forEach(b => {
                b.classList.toggle("active", b.dataset.lang === btn.dataset.lang);
                b.setAttribute("aria-pressed", b.dataset.lang === btn.dataset.lang ? "true" : "false");
            });
        });
    });
    applyTranslations(state.lang);
    document.querySelectorAll(selector).forEach(b => {
        const isActive = b.dataset.lang === state.lang;
        b.classList.toggle("active", isActive);
        b.setAttribute("aria-pressed", isActive ? "true" : "false");
    });
}

function initMobileNav() {
    const hamburger = document.getElementById("hamburger-btn");
    const nav = document.getElementById("mobile-nav");
    const overlay = document.getElementById("mobile-nav-overlay");
    const closeBtn = document.getElementById("mobile-nav-close");
    if (!hamburger || !nav || !overlay) return;

    function openNav() {
        hamburger.classList.add("is-open");
        nav.classList.add("is-open");
        overlay.classList.add("is-open");
        hamburger.setAttribute("aria-expanded", "true");
        nav.setAttribute("aria-hidden", "false");
        document.body.classList.add("nav-open");
    }

    function closeNav() {
        hamburger.classList.remove("is-open");
        nav.classList.remove("is-open");
        overlay.classList.remove("is-open");
        hamburger.setAttribute("aria-expanded", "false");
        nav.setAttribute("aria-hidden", "true");
        document.body.classList.remove("nav-open");
    }

    hamburger.addEventListener("click", openNav);
    overlay.addEventListener("click", closeNav);
    if (closeBtn) closeBtn.addEventListener("click", closeNav);

    // Close when any nav link is tapped
    nav.querySelectorAll(".mobile-nav-link, .mobile-nav-cta").forEach(el => {
        el.addEventListener("click", closeNav);
    });

    // Close on Escape key
    document.addEventListener("keydown", (e) => {
        if (e.key === "Escape" && nav.classList.contains("is-open")) closeNav();
    });
}

function initHeaderScroll() {
    const header = document.querySelector(".header");
    if (!header) return;
    window.addEventListener("scroll", () => {
        if (window.scrollY > 80) header.classList.add("scrolled");
        else header.classList.remove("scrolled");
    });
}

function initProductButtons() {
    document.querySelectorAll(".btn-card[data-product]").forEach((btn) => {
        btn.addEventListener("click", () => {
            const id = btn.dataset.product;
            const product = products[id];
            if (!product) return;
            localStorage.setItem("cdm_selected_product", JSON.stringify({ id }));
            const target = location.pathname.includes("pages/") ? "checkout.html" : "pages/checkout.html";
            window.location.href = target;
        });
    });
}

function updateCheckoutSelection() {
    const selectionEl = document.getElementById("checkout-selected");
    if (!selectionEl) return;
    const stored = localStorage.getItem("cdm_selected_product");
    const parsed = stored ? JSON.parse(stored) : null;
    const id = parsed?.id || "p1";
    const product = products[id] || products.p1;
    const nameKey = `products.${id}.title`;
    const name = translations[state.lang]?.[nameKey] || translations[state.lang]?.["products.p1.title"];
    const selectionLabel = translations[state.lang]?.["checkout.selection"] || "Produto";
    const price = product.price?.toLocaleString(state.lang, { style: "currency", currency: state.lang === "en" ? "USD" : "BRL" }) || "";
    selectionEl.textContent = `${selectionLabel} ${name} ${price ? `(${price})` : ""}`;
}

function initCheckout() {
    const btnStripe = document.getElementById("btn-stripe");
    if (!btnStripe) return;
    const stored = localStorage.getItem("cdm_selected_product");
    const parsed = stored ? JSON.parse(stored) : null;
    const id = parsed?.id || "p1";
    btnStripe.addEventListener("click", () => {
        const product = products[id] || products.p1;
        const link = product.stripeLink || products.p1.stripeLink;
        window.open(link, "_blank", "noopener");
    });
}

function renderTrackingResult(code, record) {
    const box = document.getElementById("resultado-rastreio");
    if (!box) return;
    box.classList.add("visible");
    const lang = state.lang;
    if (!record) {
        box.innerHTML = `<p><strong>${translations[lang]?.["tracking.notFound"] || "Código não encontrado."}</strong></p><p>${translations[lang]?.["tracking.help"] || "Revise o código ou fale com nosso atendimento."}</p>`;
        return;
    }
    const statusLabel = translations[lang]?.["tracking.status"] || "Status:";
    const etaLabel = translations[lang]?.["tracking.eta"] || "Previsão:";
    const foundLabel = translations[lang]?.["tracking.found"] || "Código localizado.";
    const statusText = record.status?.[lang] || record.status?.pt || "-";
    box.innerHTML = `
        <p><strong>✓ ${foundLabel}</strong></p>
        <p>${statusLabel} <strong>${statusText}</strong></p>
        <p>${etaLabel} <strong>${record.eta || "-"}</strong></p>
        <p style="color:var(--text-muted);font-size:0.82rem;">Última atualização: ${record.lastUpdate || "-"}</p>
    `;
}

function initTracking() {
    const button = document.getElementById("btn-rastrear");
    const input = document.getElementById("codigo-rastreio");
    if (!button || !input) return;
    const handler = () => {
        const code = input.value.trim().toUpperCase();
        const record = trackingDB[code];
        renderTrackingResult(code, record);
    };
    button.addEventListener("click", handler);
    input.addEventListener("keypress", (e) => {
        if (e.key === "Enter") handler();
    });
}

function initChat() {
    const existing = document.querySelector(".cdm-chat");
    if (existing) return;

    // Garantir que o fab-container existe
    let fabContainer = document.getElementById('fab-container');
    if (!fabContainer) {
        fabContainer = document.createElement('div');
        fabContainer.id = 'fab-container';
        fabContainer.className = 'fab-container';
        document.body.appendChild(fabContainer);
    }

    // Criar o container do chat
    const wrapper = document.createElement("div");
    wrapper.className = "cdm-chat";
    wrapper.innerHTML = `
        <style>
            .cdm-chat { position: fixed; right: 16px; bottom: 80px; z-index: 9998; font-family: system-ui, -apple-system, sans-serif; }
            .cdm-chat-window { width: 280px; max-height: 380px; background: #fff; border: 1px solid #FF6B6B; border-radius: 12px; box-shadow: 0 12px 30px rgba(0,0,0,0.18); display: none; flex-direction: column; overflow: hidden; }
            .cdm-chat-window.active { display: flex; }
            .cdm-chat-header { padding: 10px 12px; background: linear-gradient(135deg, #FF6B6B, #FF8E53); color: #fff; font-weight: 700; }
            .cdm-chat-body { padding: 10px; flex: 1; overflow-y: auto; background: #f7faff; }
            .cdm-chat-msg { margin-bottom: 8px; padding: 8px 10px; border-radius: 8px; background: #e8f4f8; }
            .cdm-chat-msg.you { text-align: right; background: #FF6B6B; color: white; margin-left: 40px; }
            .cdm-chat-msg.bot { text-align: left; background: #e8f4f8; margin-right: 40px; }
            .cdm-chat-input { display: flex; padding: 10px; gap: 8px; border-top: 1px solid #e5e8f0; }
            .cdm-chat-input input { flex: 1; padding: 10px; border: 1px solid #d3d8e0; border-radius: 8px; font-size: 12px; }
            .cdm-chat-input button { padding: 10px 12px; border: none; background: linear-gradient(135deg, #FF6B6B, #FF8E53); color: #fff; border-radius: 8px; font-weight: 700; cursor: pointer; font-size: 12px; }
        </style>
        <div class="cdm-chat-window" id="cdm-chat-window">
            <div class="cdm-chat-header">💬 CDM Chat</div>
            <div class="cdm-chat-body" id="cdm-chat-body"></div>
            <div class="cdm-chat-input">
                <input type="text" id="cdm-chat-input" placeholder="Mensagem...">
                <button id="cdm-chat-send">Send</button>
            </div>
        </div>
    `;
    document.body.appendChild(wrapper);

    // Criar FAB de chat
    const chatFab = document.createElement('button');
    chatFab.id = 'fab-chat';
    chatFab.className = 'fab fab-chat';
    chatFab.title = 'Abrir Chat';
    chatFab.innerHTML = '💬';

    chatFab.addEventListener('click', () => {
        const chatWindow = document.getElementById('cdm-chat-window');
        if (chatWindow) {
            chatWindow.classList.toggle('active');
        }
    });

    fabContainer.appendChild(chatFab);

    const win = document.getElementById("cdm-chat-window");
    const body = document.getElementById("cdm-chat-body");
    const input = document.getElementById("cdm-chat-input");
    const send = document.getElementById("cdm-chat-send");

    const appendMsg = (text, cls = "") => {
        const p = document.createElement("div");
        p.className = `cdm-chat-msg ${cls}`;
        p.textContent = text;
        body.appendChild(p);
        body.scrollTop = body.scrollHeight;
    };

    appendMsg(translations[state.lang]?.["chat.welcome"] || "Olá!", "bot");

    const respond = (text) => {
        const lower = text.toLowerCase().trim();
        const lang = state.lang;

        // Detecção de Rastreio (Tracking)
        if (lower.match(/rastreio|rastrear|tracking|track|código|code|pedido|order|entrega|delivery/)) {
            const responses = {
                pt: "📦 Use a aba Rastreio e cole seu código (ex: CDM123456BR). Você pode acompanhar aqui ou enviar o código que respondo!",
                en: "📦 Go to Tracking tab and paste your code (ex: CDM123456BR). You can track here or send the code!",
                es: "📦 Ve a la pestaña Rastreo y pega tu código (ej: CDM123456BR). ¡Puedes rastrear aquí o enviar el código!"
            };
            appendMsg(responses[lang] || responses.pt, "bot");
            return;
        }

        // Detecção de Pagamento & Stripe
        if (lower.match(/pagamento|payment|pagar|pay|cartão|card|pix|stripe|valor|preço|price/)) {
            const responses = {
                pt: "💳 Aceitamos Stripe com cartão crédito/débito e PIX. Totalmente seguro (PCI-DSS). Problemas? Fale com suporte!",
                en: "💳 We accept Stripe with credit/debit cards and PIX. Fully secure (PCI-DSS). Issues? Contact support!",
                es: "💳 Aceptamos Stripe con tarjeta crédito/débito y PIX. Totalmente seguro (PCI-DSS). ¿Problemas? ¡Contacta soporte!"
            };
            appendMsg(responses[lang] || responses.pt, "bot");
            return;
        }

        // Detecção de Frete & Envio
        if (lower.match(/frete|envio|shipping|entrega|quando|quando chega|quando llega|prazo/)) {
            const responses = {
                pt: "🚚 Entregamos em todo Brasil com rastreio. Prazo médio: 2-7 dias úteis. Frete: R$ 15,00 (primeira compra grátis!)",
                en: "🚚 We deliver all over Brazil with tracking. Average time: 2-7 business days. Shipping: R$ 15.00",
                es: "🚚 Entregamos en todo Brasil con rastreo. Tiempo promedio: 2-7 días hábiles. Envío: R$ 15,00"
            };
            appendMsg(responses[lang] || responses.pt, "bot");
            return;
        }

        // Detecção de Produtos
        if (lower.match(/produto|product|comprar|buy|cual|qual|o que|what|fone|carregador|cabo|headphones|charger|cable/)) {
            const responses = {
                pt: "🛍️ Temos Fone Bluetooth, Carregador USB-C e Cabo Lightning. Clique em Produtos para ver detalhes e valores!",
                en: "🛍️ We have Bluetooth Headphones, USB-C Charger, and Lightning Cable. Click Products to see details!",
                es: "🛍️ Tenemos Auriculares Bluetooth, Cargador USB-C y Cable Lightning. ¡Haz clic en Productos para ver detalles!"
            };
            appendMsg(responses[lang] || responses.pt, "bot");
            return;
        }

        // Detecção de Autenticação & Conta
        if (lower.match(/login|entrar|criar conta|account|sign up|cadastro|registrar|password|senha|usuário|user/)) {
            const responses = {
                pt: "👤 Clique no ícone azul 👤 no canto direito para Entrar ou Criar Conta. Support Google & Facebook!",
                en: "👤 Click the blue icon 👤 on the right corner to Login or Create Account. Support Google & Facebook!",
                es: "👤 Haz clic en el icono azul 👤 en la esquina derecha para Entrar o Crear Cuenta. ¡Support Google & Facebook!"
            };
            appendMsg(responses[lang] || responses.pt, "bot");
            return;
        }

        // Detecção de Carrinho
        if (lower.match(/carrinho|cart|adicionar|add|remover|remove|comprar|checkout|pedido|order/)) {
            const responses = {
                pt: "🛒 Clique no ícone 🛒 para abrir seu carrinho, adicionar produtos e finalizar compra com segurança!",
                en: "🛒 Click the 🛒 icon to open your cart, add products, and checkout safely!",
                es: "🛒 ¡Haz clic en el icono 🛒 para abrir tu carrito, agregar productos y pagar seguro!"
            };
            appendMsg(responses[lang] || responses.pt, "bot");
            return;
        }

        // Detecção de Atendimento & Suporte
        if (lower.match(/atendimento|suporte|support|ajuda|help|problema|issue|contato|contact|fone|phone|email/)) {
            const responses = {
                pt: "📞 Vá em Atendimento para falar conosco. Equipe disponível 24/7 para ajudar!",
                en: "📞 Go to Support to talk with us. Team available 24/7 to help!",
                es: "📞 Ve a Atención para hablar nosotros. ¡Equipo disponible 24/7 para ayudarte!"
            };
            appendMsg(responses[lang] || responses.pt, "bot");
            return;
        }

        // Detecção de Promoção & Cupom
        if (lower.match(/promoção|promo|cupom|coupon|desconto|discount|oferta|offer/)) {
            const responses = {
                pt: "🎉 Temos promoções especiais! Use cupom na página de checkout. Fique de olho em nossas redes!",
                en: "🎉 We have special promotions! Use coupon at checkout. Follow our social media!",
                es: "🎉 ¡Tenemos promociones especiales! Usa cupón en checkout. ¡Síguenos en redes sociales!"
            };
            appendMsg(responses[lang] || responses.pt, "bot");
            return;
        }

        // Resposta padrão amigável
        const defaultResponses = {
            pt: "😊 Ótima pergunta! Você pode explorar Produtos, Rastreio, fazer login ou falar com Atendimento. Como posso ajudar mais?",
            en: "😊 Great question! You can explore Products, Tracking, login or reach Support. How else can I help?",
            es: "😊 ¡Excelente pregunta! Puedes explorar Productos, Rastreo, iniciar sesión o contactar Atención. ¿Cómo puedo ayudarte más?"
        };
        appendMsg(defaultResponses[lang] || defaultResponses.pt, "bot");
    };

    const handleSend = () => {
        const text = input.value.trim();
        if (!text) return;
        appendMsg(text, "you");
        input.value = "";
        setTimeout(() => respond(text), 300);
    };

    send.addEventListener("click", handleSend);
    input.addEventListener("keypress", (e) => {
        if (e.key === "Enter") handleSend();
    });
}

document.addEventListener("DOMContentLoaded", () => {
    // Pequeno delay para garantir que auth.js foi carregado e estilos aplicados
    setTimeout(() => {
        // Garantir que fab-container existe
        let fabContainer = document.getElementById('fab-container');
        if (!fabContainer) {
            fabContainer = document.createElement('div');
            fabContainer.id = 'fab-container';
            fabContainer.className = 'fab-container';
            document.body.appendChild(fabContainer);
        }

        // Garantir que fab-login existe (criado por auth.js)
        if (!document.getElementById('fab-login')) {
            const loginFab = document.createElement('button');
            loginFab.id = 'fab-login';
            loginFab.className = 'fab fab-login';
            loginFab.title = 'Login / Entrar';
            loginFab.innerHTML = '👤';
            loginFab.addEventListener('click', () => {
                const modal = document.getElementById('auth-modal');
                if (modal) modal.classList.add('active');
            });
            fabContainer.appendChild(loginFab);
        }
    }, 100);

    initLanguageSwitcher();
    initMobileNav();
    initHeaderScroll();
    initProductButtons();
    initCheckout();
    initTracking();
    initChat();
});
